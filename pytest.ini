[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
asyncio_default_fixture_loop_scope = function
markers =
    integration: deterministic tests spanning multiple application components
    security: tests for untrusted-input and network safety boundaries
